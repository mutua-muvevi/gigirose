import { Box } from "@mui/material"
import { styled } from "@mui/system"

const Wrapper = styled(Box)(({ theme }) => ({

}))

const DashServices = () => {
	return (
		<Wrapper>
			<div>DashServices</div>
		</Wrapper>
	)
}

export default DashServices